var whichPathing = 1; //Could possibly use global vars for the different pathing algos
                      //Or this could be a terrible idea and really bad practice.

function testFunc(){
    console.log(whichPathing);
    whichPathing += 1;
}